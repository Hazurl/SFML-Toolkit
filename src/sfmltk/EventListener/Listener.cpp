#include <sfmltk/EventListener/Listener.hpp>

namespace sfmltk {
/*
PropagateEvent Listener::on_close(event::Closed const& closed) {
    return true; 
}

PropagateEvent Listener::on_resize(event::Resized const& resized) { 
    return true; 
}

PropagateEvent Listener::on_lost_focus(event::LostFocus const& focus) { 
    return true; 
}

PropagateEvent Listener::on_gain_focus(event::GainFocus const& focus) { 
    return true; 
}

PropagateEvent Listener::on_text_entered(event::TextEntered const& text) { 
    return true; 
}

PropagateEvent Listener::on_key_pressed(event::KeyPressed const& key) { 
    return true; 
}

PropagateEvent Listener::on_key_released(event::KeyReleased const& key) { 
    return true; 
}

PropagateEvent Listener::on_mouse_wheel_scrolled(event::MouseWheelScrolled const& mouse_wheel) { 
    return true; 
}

PropagateEvent Listener::on_mouse_button_pressed(event::MouseButtonPressed const& mouse_button) { 
    return true; 
}

PropagateEvent Listener::on_mouse_button_released(event::MouseButtonReleased const& mouse_button) { 
    return true; 
}

PropagateEvent Listener::on_mouse_moved(event::MouseMoved const& mouse_movement) { 
    return true; 
}

PropagateEvent Listener::on_mouse_entered(event::MouseEntered const& mouse_entered) { 
    return true; 
}

PropagateEvent Listener::on_mouse_left(event::MouseLeft const& mouse_left) { 
    return true; 
}

PropagateEvent Listener::on_joystick_button_pressed(event::JoystickButtonPressed const& joystick_button) { 
    return true; 
}

PropagateEvent Listener::on_joystick_button_released(event::JoystickButtonReleased const& joystick_button) { 
    return true; 
}

PropagateEvent Listener::on_joystick_moved(event::JoystickMoved const& joystick_movement) { 
    return true; 
}

PropagateEvent Listener::on_joystick_connected(event::JoystickConnected const& joystick_connection) { 
    return true; 
}

PropagateEvent Listener::on_joystick_disconnected(event::JoystickDisconnected const& joystick_disconnection) { 
    return true; 
}

PropagateEvent Listener::on_touch_began(event::TouchBegan const& touch) { 
    return true; 
}

PropagateEvent Listener::on_touch_moved(event::TouchMoved const& touch) { 
    return true; 
}

PropagateEvent Listener::on_touch_ended(event::TouchEnded const& touch) { 
    return true; 
}

PropagateEvent Listener::on_sensor(event::SensorChanged const& sensor) { 
    return true; 
}*/

}